/// App Store validation experience switch (branch `apple-validation`).
///
/// When `true`, the app boots into the **Attra Connection Lab** — an
/// AI-guided-connection experience (guided conversations, anti-ghosting coach,
/// compatibility insights, conversation games and date planning) instead of a
/// swipe-first flow. The existing dating mechanics (feed, likes, matches,
/// chats, subscriptions, visual AI, profile, monetization) remain fully
/// available under the **Discover** and **Messages** tabs.
///
/// This is a REAL user-facing experience — it is NOT reviewer-only, does not
/// detect Apple reviewers, and hides no functionality. It only reorders and
/// reframes what the user sees first.
const bool kAppStoreValidationExperience = true;
